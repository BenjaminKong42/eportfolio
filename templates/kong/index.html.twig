<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Kong, Roi de la Jungle</title>
    <style>
        body {
            background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
            color: #fff;
            font-family: 'Roboto', sans-serif;
            min-height: 100vh;
            margin: 0;
            overflow-x: hidden;
        }
        .container {
            max-width: 700px;
            margin: 80px auto;
            background: rgba(34,34,34,0.87);
            border-radius: 32px;
            box-shadow: 0 16px 32px rgba(40,40,255,0.3);
            padding: 50px 32px 48px;
            text-align: center;
            animation: popIn 1.1s cubic-bezier(.47,1.64,.41,.8);
        }
        @keyframes popIn {
            from { transform: scale(0.8) translateY(120px); opacity: 0;}
            to   { transform: scale(1)   translateY(0);     opacity: 1;}
        }
        h1 {
            font-size: 3em;
            margin-bottom: 10px;
            letter-spacing: 1.5px;
        }
        .emoji {
            font-size: 4em;
            filter: drop-shadow(0 0 6px #fff6);
            animation: pulse 1.2s infinite alternate;
            margin-bottom: 24px;
            display: block;
        }
        @keyframes pulse {
            to { transform: scale(1.085);}
        }
        ul {
            list-style: none;
            padding: 0;
        }
        li {
            background: linear-gradient(90deg, #70e1f5 0%, #ffd194 100%);
            color: #202020;
            margin: 14px 0;
            padding: 14px 22px;
            border-radius: 18px;
            font-size: 1.2em;
            box-shadow: 0 2px 6px #0003;
            font-weight: 500;
            animation: fadeIn 0.8s ease;
        }
        @keyframes fadeIn { from {opacity:0; transform:translateX(-40px);} to {opacity:1; transform:translateX(0);} }
        .footer {
            margin-top: 40px;
            font-size: 0.98em;
            color: #bbb;
        }
    </style>
</head>
<body>
    <div class="container">
        <span class="emoji">🦍</span>
        <h1>Kong, Roi de la Jungle</h1>
        <ul>
            {% for fact in facts %}
                <li>{{ fact }}</li>
            {% endfor %}
        </ul>
        <div class="footer">
            Made with <span style="color:#ff0;">★</span> by KongController (Symfony)
        </div>
    </div>
</body>
</html>